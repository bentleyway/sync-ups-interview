import SwiftUI

struct AttendeeForm: View {
    var onCreateAttendee: (Attendee) -> Void
    
    var body: some View {
        Text("Attendee Form Placeholder")
    }
}

#Preview {
    AttendeeForm() { _ in }
}
